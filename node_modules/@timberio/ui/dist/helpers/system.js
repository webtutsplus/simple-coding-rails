"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = _default;

var _reactEmotion = _interopRequireDefault(require("react-emotion"));

var _styledSystem = require("styled-system");

var _clean = _interopRequireDefault(require("./clean"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

// tldr;
// emotion(cleanedOfStyledSystem(cleanedOfPropTypes))
// Note, one option would be to define the component's proptypes and pass them in here
// so the element only has to be cleaned once, before it hits the dom
// get each styled system prop
var styleKeys = Object.keys(_styledSystem.styles);
var propTypes = Object.keys(_styledSystem.styles).filter(function (key) {
  return typeof _styledSystem.styles[key] === 'function';
}).reduce(function (a, key) {
  return Object.assign(a, _styledSystem.styles[key].propTypes);
}, {});

function _default(element) {
  // clean props from dom element
  var cleaned = (0, _clean.default)(element);
  cleaned.propTypes = propTypes;
  return function (componentStyles) {
    return (
      /*#__PURE__*/
      (0, _reactEmotion.default)(cleaned, {
        target: "e1yfs9p30"
      })( // styles passed by component
      componentStyles, // styled system comes after so it takes priority
      function (props) {
        return styleKeys.map(function (key) {
          if (typeof _styledSystem.styles[key] !== 'function') return {};
          return _styledSystem.styles[key](props);
        });
      })
    );
  };
}